{
  "version": 1,
  "comment": "Server-side publish authorization (NPJ method). The n8n publish workflow SHA-256-hashes the Bearer key presented by the app and only commits when the hash appears in `admins`. This file contains hashes only — never the key itself. Rotate by replacing the hash here (commit via the webhook or directly) and updating ADMIN_HASH in index.html.",
  "admins": [
    "e89ade35085fc8736d6b4755af45e842c6eec0c5978d318156aff6351f0fa950"
  ]
}
